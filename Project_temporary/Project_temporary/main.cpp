#include"handling.h"
#include "vhdang.h"
staffNode* headStaff;
studentNode* headStudent;

/*void addStaffFromCSV(string filename, string role)
{
	ifstream f;
	f.open(filename);
	string str;
	f >> str;
	if (str[0] == 'T')
	{
		studentNode* newStu = new studentNode;

	}
	else if (str[0] == 'F')
	f.close();
}*/

int main()
{

	return 0;
}