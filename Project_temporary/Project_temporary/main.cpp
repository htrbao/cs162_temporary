#include "handling.h"
#include "interface.h"
#include "staffInterface.h"
#include "studentInterface.h"

int main()
{	
	/*HANDLE hStdin = GetStdHandle(STD_INPUT_HANDLE);
	DWORD mode = 0;
	GetConsoleMode(hStdin, &mode);
	showInput(false, hStdin, mode);
	showInput(true, hStdin, mode);
	string a;
	cin >> a;
	cout << a;*/
	return 0;
}